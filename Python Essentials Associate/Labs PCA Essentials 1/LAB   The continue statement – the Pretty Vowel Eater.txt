Scenario
Il tuo compito qui è ancora più speciale di prima: devi riprogettare il (brutto) mangiatore di 
vocali del laboratorio precedente e creare un mangiatore di vocali migliore e aggiornato (carino)! 
Scrivere un programma che utilizzi:

un ciclo for;
il concetto di esecuzione condizionale (if-elif-else)
l'istruzione continua.

Il tuo programma deve:

chiedere all'utente di inserire una parola;
utilizzare user_word = user_word.upper() per convertire in maiuscolo la parola inserita dall'utente; 
parleremo molto presto dei metodi string e del metodo upper() - non preoccuparti;
utilizzare l'esecuzione condizionale e l'istruzione continue per "mangiare" le seguenti 
vocali A, E, I, O, U dalla parola immessa;
assegna le lettere non consumate alla variabile word_without_vowels e stampa la variabile sullo schermo.

Guarda il codice nell'editor. Abbiamo creato word_without_vowels e gli abbiamo assegnato una 
stringa vuota. Usa l'operazione di concatenazione per chiedere a Python di combinare le lettere 
selezionate in una stringa più lunga durante i turni successivi del ciclo e assegnarla alla 
variabile word_without_vowels.

Metti alla prova il tuo programma con i dati che ti abbiamo fornito.
Input              output
Gregory            GRGRY

abstemious         BSTMS


IOUEA

